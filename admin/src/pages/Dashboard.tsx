import { Typography, Flex, Checkbox } from '@strapi/design-system';

const Dashboard = () => {
  return (
    <Flex>
      <Typography>Dashboard</Typography>
    </Flex>
  );
};

export default Dashboard;
